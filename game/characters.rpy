# Character definitions. Add sprites under game/images/ (e.g. alice happy.png).

define narrator = Character(None)
define alice = Character("Alice", color="#c8ffc8")
define bob = Character("Bob", color="#c8c8ff")
